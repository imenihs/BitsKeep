# 設計解析ツールレビュー指摘（トップ階層サマリ）

最終更新: 2026-05-03
原本: `制作管理/設計解析ツールレビュー指摘.md`

## 目的
- 設計解析ツール（`bits-keep/resources/views/app/design-tools.blade.php` / `bits-keep/resources/js/pages/design-tools.js`）のUX/表示の主要指摘をトップ階層で一望できるようにする。
- 実装前提のチェックリストを設計→入力→結果→次アクション（保存）導線に即して追跡する。

## 設計レビュー指摘（要約）

### P0 / P1（主要）
- [x] 導線分断（入力と保存の距離）を短縮し、最終判定手前で次アクションが出る構成にする
- [x] ツール導線の目的別整理（タブ過多対策）を導入
- [x] バッテリー稼働の4ブロック（電池パック/動作条件/周期負荷/結果）を主要導線へ統一
- [x] 共通詳細入力と個別入力の重なりを整理し、主要入力を目的に揃える
- [x] 最終判定カードを上位化し、保存導線を明示
- [x] 英語/内部語の露出を整理し、設計者向け語に寄せる
- [x] 結果要因（支配要因/不足条件）を判断しやすい形で明示

### P2（改善）
- [x] 受動/計測/電源/保護などの結果画面密度を上げ、説明文過多を抑える
- [x] 「基底値」「ラベル」「未設定」などが判定に混ざる表示を分解
- [x] テンプレート取り込みと解析結果保存の順序を明確化
- [x] バッテリー周辺（電圧カーブ、容量/電圧下限）を結果確認に一体化

## 接頭語処理の共通化状況（実装確認）
- [x] 共通ユーティリティ `bits-keep/resources/js/utils/engineeringUnits.js` を参照して解析数値の読み書き・正規化を行う
- [x] サーバ側共通クラス `bits-keep/app/Support/EngineeringUnits.php` を追加し、スペック詳細API、スペック値正規化、部品シリーズ生成の接頭語処理を同じ規則へ寄せる
- [x] `normalizeEngineeringText` / `normalizeUnitText` / `parseEngineeringNumberDetail` / `formatSiValue` を design-tools 側で使用
- [x] 設計ツール内の入力表示変換（`numericInputValue`）を共通入力形式へ寄せる
- [x] バッテリー稼働の最終判定値は、低電力・低電流領域で接頭語付き表示へ寄せる修正を実施
- [x] `K -> k`、`µ/μ -> u`、`meg/MEG -> M相当`、`Ki/Mi/Gi/Ti`、未知接頭語拒否を JS/PHP テストで固定

## 今回追記（2026-05-03）
- [x] バッテリー稼働結果の各指標で、`0.000001` 形式の生数値が残りにくいように `formatMetricValue` / `formatRuntimeText` を導入
- [x] バッテリー稼働の警告文/メトリクスで、表示単位を明示した共通フォーマットへ統一
- [x] ページ起動/表示回帰確認（`design-tools.test.mjs`, `e2e-battery-*.spec.js`)
- [x] バッテリー稼働ロジックを `resources/js/pages/design-tools/batteryRuntime.js` へ切り出し、接頭語共通ライブラリの公開APIにJSDoc/PHPDocを追加
- [ ] 既存巨大ファイルの全量分割は残作業。今回対象のバッテリー稼働と接頭語層から分割を開始済み

## 集約前チェックリスト（確認前）
- [x] 目的別導線と全ツールの共通ワークフロー（仕様・前提→パラメータ→結果→次アクション）を保持
- [x] 旧 `false && analysisReport` 隠し出力依存の除去
- [x] 明示的 `?tool=` 指定時の優先度維持
- [x] バッテリー最終判定の可視性（`h2:バッテリー稼働` が表示）とローカルストレージ復元時動作の安定
- [x] 不足条件、支配要因、次アクションがすべて `analysisReport` に記録されること

## 監査メモ
- 主要指摘の残数は現時点で「実装側で未解決なし」想定。
- 追加検討が必要な場合は、個別シナリオ（極小負荷・超低電流）での実値可読性を定量化して再レビューを実施する。
