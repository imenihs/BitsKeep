# BitsKeep 実装進捗チェックリスト

フレームワーク: Laravel 12 / PHP 8.2+ / Vue 3 + Vite / Tailwind CSS 4 / SQLite→MySQL移行可

---

## Phase 1: 基盤構築

### 1-1. DB設計・マイグレーション
- [x] `categories` テーブル
- [x] `packages` テーブル
- [x] `spec_types` テーブル（単位候補含む）
- [x] `components` テーブル（基本情報・入手可否・ファイルパス）
- [x] `component_specs` テーブル（value_numeric含む）
- [x] `component_attributes` テーブル（キーバリュー）
- [x] `attribute_keys` テーブル
- [x] `component_category` 中間テーブル
- [x] `component_package` 中間テーブル
- [x] `locations` テーブル（階層構造対応）
- [x] `component_location` 中間テーブル
- [x] `suppliers` テーブル
- [x] `component_suppliers` テーブル（単価・商社型番・URL）
- [x] `supplier_price_breaks` テーブル
- [x] `shipping_rules` テーブル
- [x] `inventory_blocks` テーブル（ロット・リール・在庫区分）
- [x] `transactions` テーブル（入出庫履歴）
- [x] `projects` テーブル
- [x] `component_project` 中間テーブル（使用数含む）
- [x] `component_altium_links` テーブル
- [x] `audit_logs` テーブル（差分JSON）
- [ ] `user_preferences` テーブル
- [ ] `project_sync_runs` テーブル（Notion同期履歴）
- [x] usersテーブル拡張（role: admin/editor/viewer）

### 1-2. 認証・権限
- [x] Laravel Breeze導入（セッション認証）
- [x] roleミドルウェア実装（admin/editor/viewer）
- [x] ログイン画面
- [ ] 招待メール送信（ユーザー管理SCR-008）

### 1-3. 共通基盤
- [x] BaseModel（SoftDelete・audit_log自動記録 Observer）
- [x] ApiResponse共通レスポンス形式
- [x] ファイル管理ヘルパー（画像・データシート保存先ルール）
- [x] Vue 3 + Vite + Tailwind セットアップ確認・theme.css反映
- [ ] PreferenceService基盤（ホーム並び順・最近使った機能・既定案件）
- [ ] 設計解析共通レスポンス整形基盤（`summary / warnings / chart_model / next_actions`）

---

## Phase 2: コア機能実装

### 2-0. ホームダッシュボード（SCR-000）
- [x] グローバル検索ランチャー（部品/案件横断・Ctrl+K）
- [x] クイックアクション最適化（役割・ロールベース表示）
- [ ] クイックアクション並び替え保存（ユーザー別）
- [x] 今日の確認事項カード（在庫警告・最近アクセス）

### 2-1. マスタ管理（SCR-009）
- [x] 分類 CRUD API + Vue画面
- [x] パッケージ CRUD API + Vue画面
- [x] スペック種別 CRUD API + Vue画面（単位一覧含む）

### 2-2. 部品一覧（SCR-001）
- [x] 部品一覧 API（フリーワード・分類・入手可否フィルタ・ページネーション）
- [x] スペック数値範囲フィルタ（value_numeric利用）
- [x] Vue画面実装（サイドバーフィルタ・カード表示・ページネーション）
- [x] 在庫警告件数バッジ

### 2-3. 部品登録/編集（SCR-003）
- [x] 部品登録 API（POST /components）
- [x] 部品更新 API（PUT/PATCH /components/{id}）
- [x] 部品削除 API（論理削除）
- [x] スペック・属性の動的追加/削除
- [x] 画像・データシートアップロード
- [x] 仕入先・価格ブレーク登録
- [x] Vue画面実装（分類複数選択・マスタ追加ポップアップ）

### 2-4. 部品詳細（SCR-002）
- [x] 部品詳細 API（GET /components/{id}）
- [x] Vue画面実装（セクション別表示）
- [x] セクション別 PATCH API（基本情報/仕入先/スペックそれぞれ）
- [x] 全体編集 PUT API

### 2-5. 在庫入出庫
- [x] 入庫 API（POST /transactions/in）→ inventory_blocks生成
- [x] 出庫 API（POST /transactions/out）→ inventory_blocks更新・transactions記録
- [x] 出庫モーダル（SCR-002から操作）

---

## Phase 3: 在庫・棚管理

### 3-1. 保管棚管理（SCR-007）
- [x] 棚 CRUD API
- [x] Vue画面実装（棚マップ・テーブル二ペイン）
- [x] 棚卸しモード API（実数入力→差分確定）

### 3-2. 在庫警告（SCR-012）
- [x] 在庫警告一覧 API（発注点割れ部品抽出）
- [x] Vue画面実装
- [x] 発注リスト生成・CSV出力

### 3-3. 商社管理（SCR-006）
- [x] 商社 CRUD API
- [x] Vue画面実装

---

## Phase 4: 案件・比較・検索

### 4-1. 案件管理（SCR-005）
- [ ] 統合案件一覧 API（`010_xxx`〜`099_xxx` 事業配下 + 独自案件）
- [x] 案件選択候補 API（`/projects/options`）
- [ ] Notion事業DB自動発見ジョブ / API
- [ ] Notion同期履歴保存・一覧 API
- [ ] 事業一覧 API
- [x] 独自案件 CRUD API
- [x] 使用部品登録・使用数管理
- [x] コスト積算 API
- [x] Vue画面実装（案件一覧・詳細パネル・部品追加・コスト表示）

### 4-2. 部品比較（SCR-004）
- [x] 比較データ取得 API（複数ID）
- [ ] 案件選択コンボボックス共通コンポーネント
- [ ] 新規案件追加確認モーダル
- [x] Vue画面実装（差分ハイライト）

### 4-3. 類似部品検索
- [x] 類似部品検索 API（同分類・近似スペック）
- [x] Vue画面への組み込み（SCR-002 component-detail.js に fetchSimilar 追加）

---

## Phase 5: 管理・ログ・連携

### 5-1. ユーザー管理（SCR-008）
- [x] ユーザー一覧・ロール変更 API
- [x] 招待・有効化/無効化
- [x] Vue画面実装

### 5-2. 操作ログ（SCR-010）
- [x] audit_log一覧 API（フィルタ・ページネーション）
- [x] Vue画面実装（diff展開表示）

### 5-3. CSVインポート（SCR-013）
- [x] CSVパース・バリデーション API
- [x] マッピング→一括登録処理
- [x] Vue画面実装（4ステップウィザード）

### 5-4. Altium連携（SCR-014）
- [x] altium_linksテーブルCRUD API
- [x] Vue画面実装

---

## Phase 6: 付加機能

注記: 単純公式系の次点見送り候補、および `詳細仕様/仕様書.md` の `現時点では実現困難` 群は、このチェックリストには載せない。

### 6-1. 抵抗/容量ネットワーク探索（SCR-011）
- [ ] ネットワーク探索 API
- [ ] Vue画面実装（探索条件・候補比較・回路表示）
- [ ] 在庫ピッカー API連携

### 6-2. エンジニア電卓（SCR-015）
- [x] Vue画面実装（式入力・履歴・進数表示）
- [x] 定数/関数プリセット管理
- [x] 履歴保存仕様（sessionStorage）

### 6-3. 設計解析ツールハブ（SCR-016）
- [x] Vue画面実装（タブ型ツールハブ）
- [x] ADCコード/スケーリング設計 画面
- [x] 電解コンデンサ寿命推定 画面
- [ ] 誤差/歩留まり解析 API + 画面
- [x] センサ分圧/温度変換解析 画面（NTC対応）
- [x] 電流検出解析 画面
- [x] インタフェース余裕解析 画面
- [x] 電源余裕解析 画面
- [x] 比較器しきい値/ヒステリシス解析 画面
- [ ] 周波数応答/ボード線図解析 API + 画面
- [x] 熱設計 / 熱抵抗チェーン可視化 画面

### 6-4. 高頻度・小修正系ツール
- [ ] コネクタ視点補助リファレンス API/静的カタログ + 画面
- [ ] ケーブルストレート/クロス判定 API + 画面
- [ ] 0Ω/未実装/ジャンパ整理ツール API + 画面

### 6-5. 高損害診断系
- [ ] 起動・停止/リセット/依存関係診断 API + 画面
- [ ] 起動依存診断テンプレート定義（PMIC+MCU / FPGA+DDR）

### 6-6. パッケージ管理
- [ ] パッケージ画像・PDF管理
- [ ] Vue画面実装

---

## Phase 7: 品質・本番対応

- [ ] E2Eテスト（主要フロー: 登録→入庫→出庫→警告）
- [ ] バリデーション網羅確認（API境界値）
- [ ] 本番環境デプロイ手順確立（Nginx + PHP-FPM）
- [ ] ファイルストレージ本番設定（画像・PDF保存先）
- [ ] バックアップ設計（DB・ファイル）
- [ ] README.md 本番運用手順追記
